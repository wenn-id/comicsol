# ZCode live agent-host smoke record — 2026-08-30

This record documents the ZCode lane of WP5. It follows the field set and verification
threshold in [`docs/agent-host-smoke.md`](../agent-host-smoke.md). It is a **partial /
blocked** record: the installer lifecycle was exercised and retained, but live image
execution was honestly blocked in this run, so ZCode remains Experimental.

- **Agent host name and version:** ZCode, `zcode` CLI `0.16.3`, invoked from the app's
  bundled headless entrypoint `zcode.cjs`.
- **Comic Sol commit or version:** commit `9b47561c810db88a047e837f96594290ebe5b198`,
  version `2.0.0rc6`, installed from the locally built wheel
  `comic_sol-2.0.0rc6-py3-none-any.whl`.
- **Installation target and scope:** target `zcode`, user scope. Placement observed at
  `${ZCODE_HOME}/skills/comic-sol` inside a disposable home. The install marker
  `.comic-sol-install.json` records target, scope, version, bundle digest, and managed
  relative paths only. The host's skill discovery confirmed the skill at that location
  via `zcode skills list`.
- **Filesystem capability:** the host read and wrote its disposable skill destination and
  plugin cache. This lane did not create a generated Comic Sol project because live
  execution did not run (see below).
- **Shell/tool-execution capability:** the headless `zcode.cjs --prompt` route was
  attempted but could not start a model session: the CLI requires a model provider
  config that resolves in the host home. On this device the only usable model config and
  its credential live in the user's interactive Windows ZCode session. Reproducing that
  provider config inside the disposable home would have meant copying the user's
  credentials, which Comic Sol and the WP5 evidence rules forbid, and reusing the live
  Windows session home would have meant running the lane in the user's interactive
  profile, which is not authorized for this autonomous run. ZCode's supported live
  execution surface is its GUI app rather than an isolated headless CLI on this device.
- **Image-generation route:** **honestly blocked** in this run. No live generation
  occurred, and no image route is claimed.
- **Portable-handoff route:** **not applicable** in this run because no host-side job
  could be executed; no handoff archive was produced by the blocked lane.
- **Installer lifecycle result (retained):**
  - install → recorded destination `<home>/.zcode/skills/comic-sol`, marker written;
  - repeat install → idempotent, no duplicate or corruption;
  - upgrade over the installed bundle → replaced cleanly under the same marker scope;
  - uninstall → removed only the marker's managed paths and left an unrelated user skill
    (`unrelated-zcode-skill`) intact.
  - The lane also exercised codex and antigravity targets with the same lifecycle;
    this record is limited to the zcode target.
- **Output evidence:** no generated project, image, page PNG, or PDF exists for the ZCode
  lane because live execution was blocked. Retained evidence is the sanitized execution
  log and the installer lifecycle observations described above.
- **Execution date supplied by the tester:** 2026-08-30 (Asia/Jakarta, UTC+07:00).
  The retained local ZCode lane log starts with
  `=== ZCode live run started 2026-08-30T04:59:22+07:00 ===`. GitHub's
  August 29 UTC display timestamp corresponds to August 30 in UTC+07:00.
- **Known limitations (exact missing evidence):**
  - No live host execution record: the headless CLI could not start a model session in an
    isolated home without copying the user's provider credentials.
  - No project manifest, QA report, page PNG, PDF, or raster for ZCode.
  - The host's GUI (its supported interactive surface) was not driven in this autonomous
    run and is not claimed.
  - No durable, access-controlled evidence links were produced; all retained material is
    local to the tester's machine.
  - ZCode remains **Experimental**. The verified thresholds (host read/wrote a Comic Sol
    project, completed generation or portable-handoff intake, validated and exported) are
    not satisfied here.
