services:
  comic-sol:
    build: .
    image: comic-sol:2.0.0rc3
    read_only: true
    command: ["mcp", "--root", "/data"]
    volumes:
      - comic-sol-data:/data
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true

volumes:
  comic-sol-data:
